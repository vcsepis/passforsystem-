apiVersion: v1
kind: Service
metadata:
  name: {{ include "development.fullname" . }}
  labels:
    {{- include "development.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "development.selectorLabels" . | nindent 4 }}
