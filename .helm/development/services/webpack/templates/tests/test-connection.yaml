apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webpack.fullname" . }}-test-connection"
  labels:
    {{- include "webpack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webpack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
