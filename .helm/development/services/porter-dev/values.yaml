# Default values for development.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nguyenthang98/passforsystem_porter
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0.0.1"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

env:
    - name: SERVER_URL
      value: "http://localhost:8080" 
    - name: SERVER_PORT
      value: "8080"
    - name: DB_HOST
      value: paas-dev-postgresql
    - name: DB_PORT
      value: 5432
    - name: DB_USER
      value: postgres
    - name: DB_PASS
      value: test-password
    - name: DB_NAME
      value: porter
    - name: SQL_LITE
      value: false
    - name: DISABLE_ALLOWLIST
      value: true
    - name: WELCOME_FORM_WEBHOOK
      value: http://localhost:8080
    - name: GITHUB_CLIENT_ID
      value: 3614f9c923f8925c69db
    - name: GITHUB_CLIENT_SECRET
      value: 7e35da28445e3441ac97a8be2c22952add38d4a9
    - name: GITHUB_APP_CLIENT_ID
      value: Iv1.62e6cb85121c72d9
    - name: GITHUB_APP_CLIENT_SECRET
      value: 2309f7dc4b102fd4f331c468eb3757f767b9a41b
    - name: GITHUB_APP_WEBHOOK_SECRET
      value: abc123
    - name: GITHUB_APP_NAME
      value: GITHUB_APP_NAME
    - name: GITHUB_APP_ID
      value: GITHUB_APP_ID
    - name: GITHUB_APP_SECRET_PATH
      value: /porter/.cred/github_app_private_key.pem
