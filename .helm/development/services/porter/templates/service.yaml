apiVersion: v1
kind: Service
metadata:
  name: {{ include "paas-dev-porter.fullname" . }}
  labels:
    {{- include "paas-dev-porter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paas-dev-porter.selectorLabels" . | nindent 4 }}
